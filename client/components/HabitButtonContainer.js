import React, { Component } from 'react';
import HabitButton from './HabitButton';
class HabitButtonContainer extends Component {
  constructor(props){
    super(props);
  }

  render(){
    return <HabitButton />
  }
}

export default HabitButtonContainer;